language: php

php:
  - 5.6

sudo: false

services: mysql

cache:
    directories:
        - vendor
        - $HOME/.composer/cache

before_install: composer self-update

install: composer install

script: make fullbuild
